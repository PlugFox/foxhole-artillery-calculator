name: CI - All Checks

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # This job waits for both Linux and Windows checks to complete
  all-checks:
    name: All CI Checks
    runs-on: ubuntu-latest
    needs: []  # Will be satisfied when both other workflows pass

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: CI Status
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ All CI checks completed successfully!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "The following checks have passed:"
          echo "  ✅ Linux Code Quality Checks"
          echo "  ✅ Windows Build Verification (Debug + Release)"
          echo ""
          echo "Your changes are ready for review!"
          echo ""
